# Copy this file to .env in the project root and fill in your actual API key.
BROWSERBASE_API_KEY="bbbb_live_cwm1qO82OfDjxgtRT7IPRg7qnMs"

# Optional: If your API is hosted elsewhere or on a different port, specify its base URL.
# This is used to construct the callback URL for Browserbase.
# Defaults to "http://localhost:3000" if not set in config.ts.
# API_BASE_URL="https://your-publicly-accessible-api-domain.com"

# This is automatically set up by 'prisma init'.
# For SQLite, it points to a local file. Example: DATABASE_URL="file:./prisma/dev.db"
DATABASE_URL="file:./prisma/dev.db"

# Optional: Port for the application to run on. Defaults to 3000 in config.ts.
# PORT=3000
